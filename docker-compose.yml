services:
  web-security-proxy:
    build: .
    ports:
      - "8080:8080"
      - "8888:8888"
    volumes:
      - proxy-data:/app/data
    environment:
      - CONFIG_PATH=/app/config.yaml
    restart: unless-stopped

volumes:
  proxy-data:
